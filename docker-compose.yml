services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        USERNAME: ${USERNAME:-vscode}
        USER_UID: ${USER_UID:-1000}
        USER_GID: ${USER_GID:-1000}
    # 旧 runArgs 相当
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [ gpu ]
    # shared memory size
    shm_size: "8g"
    # 常駐コマンド
    tty: true
    stdin_open: true
    command: [ "bash", "-lc", "sleep infinity" ]

    # 旧 workspaceMount 相当（compose側で正本化）
    volumes:
      - .:/workspace:cached
      - datasets:/datasets:ro
      - train_logs:/train_logs

    working_dir: /workspace
    environment:
      - DATA_ROOT=/datasets
      - LOG_ROOT=/train_logs

volumes:
  datasets:
    driver: local
    driver_opts:
      type: "nfs"
      o: "addr=${NFS_DATA_ADDR:-192.168.128.9},nfsvers=4,ro"
      device: ":${NFS_DATA_EXPORT}"

  train_logs:
    driver: local
    driver_opts:
      type: "nfs"
      o: "addr=${NFS_LOGS_ADDR:-192.168.128.248},rw,noatime"
      device: ":${NFS_LOGS_EXPORT}"
